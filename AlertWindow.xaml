<Window x:Class="InteligentnyPosrednikNieruchomosci.AlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InteligentnyPosrednikNieruchomosci"
        mc:Ignorable="d"
        Title="AlertWindow" MinHeight="200" MaxHeight="200" Height="200" Width="250" MaxWidth="250" MinWidth="250">
    <Grid Background="LightBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" TextAlignment="Center" Grid.RowSpan="2" Text="{Binding AlertText}" TextWrapping="Wrap" HorizontalAlignment="Center"  FontSize="15" VerticalAlignment="Center" Height="80" Width="200" />
        <Button Grid.Row="2" Grid.RowSpan="2" Command="{Binding AlertButton}" Content="ZAMKNIJ" HorizontalAlignment="Center" VerticalAlignment="Center" Width="120" Height="50" Background="SteelBlue"  FontSize="18"  />
    </Grid>
</Window>
